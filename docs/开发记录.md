- **2026-06-24**：继续推进 `docs/40.后台管理模块-模型管理页面后续开发文档.md`（40 号后台模型管理后续开发文档）的第 3 步，完成模拟面试主流式链路的最小 `modelId`（模型标识）接入：`backend/src/types/interview.ts`（后端面试流类型文件）与 `frontend/src/services/sse/sse-types.ts`（前端流式类型文件）新增可选 `modelId`（模型标识），`frontend/src/composables/showcase/useMockInterviewSpaceMockState.ts`（宇宙模拟面试状态组合函数）在发起 `POST /api/interview/stream`（模拟面试流式接口）时会透传当前 `selectedModelId`（当前选中模型标识），`backend/src/providers/remote-llm-provider.ts`（远程大模型提供器）则改为调用 `resolveActiveModelConfig(request.modelId)`（按请求模型标识解析当前可用模型配置函数），从而让 `remote`（远程模型）模式真正按当前所选模型生成流式反馈。此次改动仅扩展请求体字段，不调整接口路径，也不修改 `SSE`（服务端发送事件）`chunk/done/error`（分片/完成/错误）结构；`mock`（本地模拟）与现有恢复链路不受影响。同步更新 `docs/40.后台管理模块-模型管理页面后续开发文档.md`（40 号后台模型管理后续开发文档），将流式接入状态改为“前端与 Node 已接入，`cpp`（C++ 引擎）链路待三方评审后决定是否继续消费该字段”。- **2026-06-24**：继续推进 `docs/40.后台管理模块-模型管理页面后续开发文档.md`（40 号后台模型管理后续开发文档）的第 2 步，正式把报告生成链路切到“按当前选中 `modelId`（模型标识）执行”的后端闭环：`frontend/src/composables/showcase/useMockInterviewSpaceMockState.ts`（宇宙模拟面试状态组合函数）现在会把 `questionReviews`（逐题复盘）、`sourceDocumentExcerpt`（资料片段）、`weaknessFocusAreas`（弱项聚焦维度）、`suggestedFocus`（建议关注点）与 `summaryBody`（摘要正文）一并透传给 `generateRemoteInterviewReport`（生成远端报告函数）；`backend/src/services/report-service.ts`（报告服务文件）改为先生成本地规则基线摘要，再通过新增的 `build-report-llm-messages.ts`（报告提示词构造文件）+ `parse-report-llm-json.ts`（报告结构化解析文件）调用 `completeRemoteLlmJson`（远程大模型 JSON 请求工具），按 `resolveActiveModelConfig(modelId)`（按模型标识解析运行时配置函数）真正走当前所选模型生成 `summaryHeadline`（摘要标题）、`summaryBody`（摘要正文）、`primaryWeakness`（主要弱项）、`weaknessTags`（弱项标签）、`weaknessFocusAreas`（弱项聚焦维度）、`suggestedFocus`（建议关注点）与 `practicePlan`（专项计划）；若远程模型不可用、返回空结果或解析失败，则自动回退到原本本地摘要逻辑，不影响报告落库和前端展示。同步补齐 `backend/src/types/report.ts`（报告类型文件）、`backend/src/controllers/report-controller.ts`（报告控制器文件）与 `frontend/src/services/interview/interview-report-api.ts`（前端报告接口文件）的请求/响应字段映射。验证：执行 `npm run build`（执行生产构建）通过，未新增前端构建错误；本轮仍未改动 `POST /api/interview/stream`（模拟面试流式接口）与 `SSE`（服务端发送事件）`chunk/done/error`（分片/完成/错误）契约。
# 2026-06-24 新增后台模型管理后续开发文档

- **2026-06-24**：新增 `docs/40.后台管理模块-模型管理页面后续开发文档.md`（40 号后台模型管理后续开发文档），承接 `docs/37.后台管理模块-模型管理页面开发文档.md`（后台模型管理页面开发文档）的第一阶段落地结果，明确后续重点转向 `RemoteLLMProvider`（远程大模型提供器）读取后台默认模型、`GET /api/models/enabled`（启用模型列表接口）、用户侧模型选择、密钥安全升级与模型管理审计；文档继续约束不单方修改 `POST /api/interview/stream`（模拟面试流式接口）和 `SSE`（服务端发送事件）契约，避免模型选择能力直接冲击前端、Node 网关与 C++ 的冻结协作边界。
- **2026-06-24**：继续推进 40 号文档的后端最小闭环：新增 `backend/src/services/model-config-resolver.ts`（模型配置解析服务文件），统一收口“指定模型 → 后台启用默认模型 → 后台任一启用模型 → `INTERVIEW_REMOTE_*`（远程模型环境变量）”的解析优先级；`backend/src/providers/remote-llm-provider.ts`（远程大模型提供器）改为优先消费后台模型配置，并在模型未启用流式或缺少 `apiKey`（接口密钥）时返回明确错误；同时新增 `GET /api/models/enabled`（启用模型列表接口），仅向用户侧暴露 `modelId`（模型标识）、`displayName`（展示名称）、`provider`（模型供应商）与 `isDefault`（是否默认）四个安全字段。整个过程未修改 `POST /api/interview/stream`（模拟面试流式接口）请求体和 `SSE`（服务端发送事件）载荷契约。
- **2026-06-24**：继续推进 40 号文档的用户侧接入：新增 `frontend/src/services/model/model-api.ts`（用户侧模型接口文件）与 `frontend/src/composables/model/useEnabledModels.ts`（启用模型组合函数），在 `frontend/src/views/showcase/mock-interview-space.vue`（宇宙模拟面试页主文件）和 `SpaceHeader.vue`（宇宙页头组件）中接入轻量模型选择入口；当前选择仅保存 `modelId`（模型标识），不持久化完整模型配置，也不把 `apiKey`（接口密钥）或 `baseUrl`（接口基础地址）暴露到浏览器侧。
- **2026-06-24**：同步为两条非流式链路补齐可选 `modelId`（模型标识）透传：`generateRemoteInterviewReport`（生成远端报告函数）与 `generatePracticePool`（生成补练题池函数）前后端类型已兼容接收该字段；其中补练题链路已继续下沉到 `backend/src/utils/complete-remote-llm-json.ts`（远程大模型 JSON 请求工具），会通过 `resolveActiveModelConfig`（解析当前可用模型配置函数）真正按所选模型调用远程模型；报告生成链路当前仍以本地摘要服务为主，仅先保存 / 透传 `modelId`（模型标识）供后续审计和真实远程摘要接入使用。

# 2026-06-23 新增后台管理模块模型管理页面开发文档

- **2026-06-23**：对 38 号方案 `Phase 2`（第二阶段）执行边界纠偏：此前曾把参数保真兜底误落到 `frontend/src/views/workbench/**`（旧工作台页面目录），与仓库“旧工作台冻结、不再开发”的约束冲突；现已全部回撤相关代码与 `route-context-cache`（路由上下文缓存）试验文件，并把 `docs/38.路由守卫三层治理与参数保真方案.md`（路由守卫三层治理与参数保真方案文档）修正为“后续仅允许落到全局路由层或 `mock-interview-space`（宇宙模拟面试页）主链路”。
- **2026-06-23**：按最新边界正式移除旧工作台壳层：删除 `/workspace`（旧工作台路由）接入、`frontend/src/views/workbench/*.vue`（旧工作台页面文件）与 `frontend/src/components/workbench/*`（旧工作台专用组件文件）；同时保留 `frontend/src/composables/workspace/useWorkbenchPersistence.ts`（工作台持久化组合函数）、`frontend/src/composables/workspace/useOverviewLaunchState.ts`（总览启动状态组合函数）、`frontend/src/composables/workspace/useLibraryWorkspaceState.ts`（资料库工作区状态组合函数）及 `frontend/src/views/workbench/*.data.ts`（旧目录下仍被宇宙页复用的数据文件），避免触碰 `mock-interview-space`（宇宙模拟面试页）现有状态链。
- **2026-06-23**：继续推进 `docs/38.路由守卫三层治理与参数保真方案.md`（路由守卫三层治理与参数保真方案文档）并执行 `Phase 1`（第一阶段）止血落地：新增 `frontend/src/router/route-guard-utils.ts`（路由守卫工具文件），将公共路由判定、客户端登录态清理、鉴权失败冷却和未登录 / 权限不足重定向构造从 `frontend/src/router/permission.ts`（路由守卫文件）中拆出；守卫主流程改为单返回值模式，并补上“本地有登录态但无 `token`（令牌）时不再请求 `/api/auth/me`（当前登录态接口）”“`/me` 失败后立即清理本地状态并进入短冷却窗口”的止血策略，用于阻断近期出现的 401 风暴与重复跳转循环。
- **2026-06-23**：新增 `docs/38.路由守卫三层治理与参数保真方案.md`（路由守卫三层治理与参数保真方案文档），针对近期暴露的 `/api/auth/me`（当前登录态接口）401 风暴、路由守卫死循环和跳转参数丢失问题，按“三层方案”收敛治理方向：第一层区分公共路由、静态权限路由与动态路由；第二层收口守卫单次导航结果、登录态短路、`query`（查询参数）优先和 `params`（路径参数）恢复兜底；第三层沉淀统一守卫工具、动态路由管理、元信息规范与监控口径，用于后续后台模块和动态菜单扩展。
- **2026-06-23**：根据 `docs/37.后台管理模块-模型管理页面开发文档.md`（后台模型管理页面开发文档）落地后台模型管理第一阶段：新增 `/admin/models`（模型管理页）路由与侧边栏入口；新增 `frontend/src/views/admin/models.vue`（后台模型管理页）和 `frontend/src/services/admin/admin-api.ts`（后台接口服务）的模型列表、新增、编辑、启停、设默认、连通测试能力；新增 `backend/src/types/model-config.ts`（模型配置类型）与 `backend/src/storage/model-config-store.ts`（模型配置存储），并在 `backend/src/controllers/admin-controller.ts`（后台控制器）/ `backend/src/routes/admin-routes.ts`（后台路由）/ `backend/src/services/admin-service.ts`（后台服务）中接入真实后台接口闭环。当前仍未改动 `POST /api/interview/stream`（模拟面试流式接口）契约，也未把 `modelId`（模型标识）接入主面试链路。
- **新增文档**：`docs/36.1.后台管理模块-模型管理页面开发文档.md`（后台模型管理页面开发文档）。
- **目标**：在现有后台管理模块中规划 `模型管理`（大模型配置管理）页面，用于集中维护用户可选的大模型配置，支持新增、编辑、停用、设置默认模型和连通性测试，减少用户手动填写模型参数的成本。
- **边界**：第一阶段只做模型配置管理闭环，不修改 `POST /api/interview/stream`（模拟面试流式接口）与 `chunk/done/error`（分片 / 完成 / 错误）事件契约；如后续要把 `modelId`（模型标识）加入流式请求体，必须先走前后端与 C++（后端语言）协作变更流程。
- **安全约束**：文档明确 `apiKey`（接口密钥）只允许后端接收和存储，前端只展示脱敏值，正式环境应迁移到加密落盘或密钥服务。
- **2026-06-23**：新增 `docs/39.38号文档Phase2-宇宙页URL参数设计稿.md`（38 号文档 Phase 2 宇宙页 URL 参数设计稿文档），作为 `docs/38.路由守卫三层治理与参数保真方案.md`（路由守卫三层治理与参数保真方案文档）的第二阶段衍生设计稿，专门收敛 `mock-interview-space`（宇宙模拟面试页）主链路的 URL（统一资源定位符）参数边界；文档明确宇宙页先采用“最小 URL 协议”路线，优先落地 `scene`（场景参数），保留现有 `welcome`（欢迎入场标记），按需扩展 `reportSessionId`（报告会话标识）、`mockSessionId`（模拟会话标识）与 `threadId`（题线程标识），并强调复杂业务态继续留在 `useWorkbenchPersistence`（工作台持久化组合函数）而不进入 `query`（查询参数）。
- **2026-06-23**：按 `docs/39.38号文档Phase2-宇宙页URL参数设计稿.md`（38 号文档 Phase 2 宇宙页 URL 参数设计稿文档）完成第一批最小落地：仅在 `frontend/src/views/showcase/mock-interview-space.vue`（宇宙模拟面试页主文件）接入 `scene`（场景参数）协议，建立“`query.scene`（查询参数场景值）优先、`sourcePage`（来源页标识）回退、默认 `overview`（总览场景）兜底”的初始化规则；新增统一 `replaceSceneQuery`（场景查询参数同步函数）与 `openSceneById`（统一场景打开函数），让轨道切换、资料库进模拟、练习进模拟、完成面试进报告等主入口统一同步 URL（统一资源定位符）与本地持久化上下文；同时保留 `welcome`（欢迎入场标记）的一次性消费语义，删除它时不丢失新的 `scene`（场景参数）。
- **2026-06-23**：继续按 `docs/39.38号文档Phase2-宇宙页URL参数设计稿.md`（38 号文档 Phase 2 宇宙页 URL 参数设计稿文档）落地第二批最小能力：为报告场景补齐 `scene=report&reportSessionId=<sessionId>`（报告场景直达参数）协议。`frontend/src/views/showcase/mock-interview-space.vue`（宇宙模拟面试页主文件）新增 `reportSessionId`（报告会话标识）查询参数解析与 `replaceReportSceneQuery`（报告场景查询参数同步函数），完成面试后进入报告场景时会显式写回对应 `sessionId`（会话标识）；`useMockInterviewSpaceReportHydration.ts`（报告场景水合组合函数）与 `useMockInterviewSpaceReportScene.ts`（报告场景组合函数）同步支持“指定报告会话优先于最近报告”规则，使外部直达、刷新恢复与宇宙页内的“打开报告”入口都能稳定落到目标报告。
- **2026-06-24**：继续推进 `docs/39.38号文档Phase2-宇宙页URL参数设计稿.md`（38 号文档 Phase 2 宇宙页 URL 参数设计稿文档）的第三批最小收口：将 `mockSessionId`（模拟会话标识）从 `frontend/src/views/showcase/mock-interview-space.vue`（宇宙模拟面试页主文件）的路由层真正接入 `frontend/src/composables/showcase/useMockInterviewSpaceMockState.ts`（模拟状态组合函数），让 `scene=mock&mockSessionId=<sessionId>`（模拟场景直达参数）可以直接拉起指定已完成历史会话的预览态；同时补齐主页面 `replaceSceneQuery`（场景查询参数同步函数）对 `mockSessionId`（模拟会话标识）的清理规则，并新增“历史预览会话变化时自动回写 `mockSessionId`（模拟会话标识）”的同步监听，避免离开 `mock`（模拟场景）后遗留旧参数。当前 `threadId`（题线程标识）仍未进入 URL（统一资源定位符）协议，按设计稿仍属于后续可选扩展。
- **2026-06-24**：继续按 `docs/38.路由守卫三层治理与参数保真方案.md`（路由守卫三层治理与参数保真方案文档）与 `docs/39.38号文档Phase2-宇宙页URL参数设计稿.md`（38 号文档 Phase 2 宇宙页 URL 参数设计稿文档）收口 `Phase 2`（第二阶段）宇宙页主链路：为 `frontend/src/views/showcase/mock-interview-space.vue`（宇宙模拟面试页主文件）补齐 `threadId`（题线程标识）查询参数解析、回写与离场清理，并将 `mockThreadIdOverride`（模拟题线程覆盖值）下沉到 `frontend/src/composables/showcase/useMockInterviewSpaceMockState.ts`（模拟状态组合函数）；新增 `openHistoryPreviewBySessionAndThread`（按会话与题线程打开历史预览函数），使 `scene=mock&mockSessionId=<sessionId>&threadId=<threadId>`（模拟场景历史题线程直达参数）可以在刷新与外部直达后稳定定位到指定历史题线程。至此宇宙页主链路的最小 `query`（查询参数）协议已覆盖 `scene`（场景参数）、`reportSessionId`（报告会话标识）、`mockSessionId`（模拟会话标识）与 `threadId`（题线程标识），`Phase 2`（第二阶段）剩余未落地项主要转为全局路由层的 `params`（路径参数）恢复兜底与未来动态路由补匹配能力。
- **2026-06-24**：开始推进 `docs/38.路由守卫三层治理与参数保真方案.md`（路由守卫三层治理与参数保真方案文档）的 `Phase 3`（第三阶段）最小落地：先在 `frontend/src/router/child-routes.ts`（子路由文件）与 `frontend/src/router/routes.ts`（根路由文件）中为现有登录页、后台页、宇宙页、聊天页和 404 页补齐显式 `meta`（路由元信息）语义，统一标注 `public`（公共路由）、`guestOnly`（仅游客可进）、`requiresAuth`（需要鉴权）、`requiredRole`（所需角色）、`dynamic`（是否动态路由）与 `preserveQuery`（保留查询参数）等字段；同时将 `frontend/src/router/permission.ts`（路由守卫文件）对这些元信息的读取方式收拢为显式 `metaRecords`（元信息列表）扫描，为后续继续抽离动态路由管理器与统一监控口径打基础。当前仍未落地 `dynamic-route-manager.ts`（动态路由管理文件）与路由监控埋点，属于 `Phase 3`（第三阶段）后续项。
- **2026-06-24**：收口宇宙页登录后首屏场景漂移问题：此前 `frontend/src/views/showcase/mock-interview-space.vue`（宇宙模拟面试页主文件）在没有显式 `scene`（场景参数）时仍会回退读取 `sourcePage`（来源页标识）持久化上下文，导致用户退出再登录后可能被恢复到 `library`（资料库场景）或 `feedback`（反馈场景）；同时 `frontend/src/views/login.vue`（登录页）会直接消费守卫保留下来的 `redirect`（回跳参数），把旧的宇宙页 `scene`（场景参数）原样带回。现已统一改为：普通登录页若回跳目标是 `/showcase/mock-interview-space`（宇宙模拟面试页路由），则强制落到默认总览入口 `?welcome=1`（欢迎入场标记）；宇宙页自身初始化在没有显式 `scene`（场景参数）时固定返回 `overview`（总览场景），不再从 `sourcePage`（来源页标识）推导首屏场景。这样保留显式深链直达能力，但普通登录进入宇宙页时行为稳定为总览首屏。
- **2026-06-24**：继续推进 `docs/38.路由守卫三层治理与参数保真方案.md`（路由守卫三层治理与参数保真方案文档）剩余全局路由层能力：新增 `frontend/src/router/dynamic-route-manager.ts`（动态路由管理文件）作为统一动态路由就绪入口，当前先提供幂等 `ensureDynamicRoutesReady`（确保动态路由已就绪函数）骨架，避免未来把动态菜单注册逻辑重新散回守卫主流程；新增 `frontend/src/router/route-navigation-metrics.ts`（路由导航指标文件），统一统计 `/api/auth/me`（当前登录态接口）探测次数、守卫重定向次数、404 命中次数与路由切换次数；新增 `frontend/src/router/route-param-recovery.ts`（路由参数恢复文件），为显式标注 `persistParams`（持久化路径参数）的路由提供 `sessionStorage`（会话级存储）恢复兜底。`frontend/src/router/permission.ts`（路由守卫文件）现已接入这三块能力：动态路由预热、鉴权探测埋点、守卫跳转埋点、404 记录与 `params`（路径参数）恢复重导向。当前仓库尚未有真实动态菜单路由和已启用 `persistParams`（持久化路径参数）的页面，因此这轮主要先把全局治理骨架落地。
- **2026-06-24**：为全局 `persistParams`（持久化路径参数）机制接入第一条真实业务闭环：新增后台报告详情路由 `/admin/reports/:sessionId`（后台报告详情路径参数路由），命名为 `AdminReportDetail`（后台报告详情路由名），并显式标注 `persistParams: true`（持久化路径参数）；`frontend/src/views/admin/reports.vue`（后台报告页）同步改为“列表页 + 路由详情态”一体，点击“查看详情”时通过 `router.push`（路由跳转）写入 `sessionId`（会话标识）路径参数，关闭详情时回退到 `AdminReports`（后台报告列表路由名），页面刷新后则通过 `useRoute`（当前路由对象）读取 `route.params.sessionId`（当前路由会话标识参数）重新拉起详情。这样 `route-param-recovery.ts`（路由参数恢复文件）已有了第一条可见验证路径，可用于验证 `params`（路径参数）在刷新后的恢复兜底行为。
- **2026-06-24**：继续为全局 `persistParams`（持久化路径参数）机制接入第二条真实业务闭环：新增后台会话详情路由 `/admin/sessions/:sessionId/:threadId`（后台会话详情双路径参数路由），命名为 `AdminSessionDetail`（后台会话详情路由名），并显式标注 `persistParams: true`（持久化路径参数）；`frontend/src/views/admin/sessions.vue`（后台会话页）同步改为“列表页 + 路由详情态”一体，点击“查看详情”时通过 `router.push`（路由跳转）写入 `sessionId`（会话标识）与 `threadId`（题线程标识）双参数，关闭详情时回退到 `AdminSessions`（后台会话列表路由名），页面刷新后则通过 `useRoute`（当前路由对象）读取 `route.params.sessionId`（当前路由会话标识参数）与 `route.params.threadId`（当前路由题线程标识参数）重新拉起详情。至此 `route-param-recovery.ts`（路由参数恢复文件）已覆盖单参数和双参数两类真实详情场景，可继续作为后续详情页路由化的模板。
- **2026-06-24**：继续把 `docs/38.路由守卫三层治理与参数保真方案.md`（路由守卫三层治理与参数保真方案文档）里的路由监控口径做成可见调试出口：为 `frontend/src/router/route-navigation-metrics.ts`（路由导航指标文件）补充 `resetRouteNavigationMetrics`（重置路由导航指标函数），并在 `frontend/src/router/index.ts`（路由入口文件）的 `setupRouter`（路由初始化函数）里挂载 `window.__routeMetrics`（浏览器全局路由指标调试对象），提供 `getSnapshot`（获取指标快照函数）与 `reset`（重置函数）；同时在 `frontend/src/types/global.d.ts`（浏览器全局类型声明文件）中补齐类型声明。这样开发期可直接在控制台调用 `window.__routeMetrics.getSnapshot()`（获取当前路由指标快照）观察 `/api/auth/me`（当前登录态接口）探测次数、守卫重定向次数、404 命中次数与路由切换次数，不必额外接入页面调试面板。
- **2026-06-24**：补齐后台详情路由收口体验：`frontend/src/views/admin/layout.vue`（后台布局页）里的侧边栏导航现为每个菜单项显式区分 `name`（导航分组名）与 `routeName`（实际跳转路由名），从 `AdminSessionDetail`（后台会话详情路由名）或 `AdminReportDetail`（后台报告详情路由名）点击“会话管理 / 报告管理”时会明确回到列表路由，而不是停留在当前详情态；同时保留原有 `isNavItemActive`（导航激活态判断函数）分组高亮规则，确保详情页仍高亮所属菜单。这样后台 `persistParams`（持久化路径参数）真实闭环不仅覆盖“详情可直达、刷新可恢复、关闭可回列表”，也覆盖“侧边栏回列表”这一日常操作路径。
- **2026-06-24**：继续收口 `docs/38.路由守卫三层治理与参数保真方案.md`（路由守卫三层治理与参数保真方案文档）里最后剩余的动态路由治理尾巴：将 `frontend/src/router/dynamic-route-manager.ts`（动态路由管理文件）从空骨架升级为真实注册入口，先把后台详情路由 `AdminSessionDetail`（后台会话详情路由名）与 `AdminReportDetail`（后台报告详情路由名）统一收拢为动态子路由集合，并通过 `router.addRoute`（动态注册路由）挂载到 `AdminRoot`（后台根路由名）下；`frontend/src/router/permission.ts`（路由守卫文件）同步改为仅在 `/admin`（后台路径前缀）下出现未完整匹配时预热动态路由，避免每次后台列表页都重复走预热；`frontend/src/router/child-routes.ts`（子路由文件）则回退为只保留后台静态列表路由。这样 38 号文档中的 `dynamic-route-manager.ts`（动态路由管理文件）已经不再是“只有状态位的骨架”，而是具备第一批真实业务动态注册能力。

# 2026-06-08 新增前后端接口对齐文档

- **目标**：为 C++（后端语言）重构同学生成一份可直接对齐的前后端接口文档，明确当前 HTTP（超文本传输协议）请求契约、`SSE`（服务端发送事件）推送契约、数据类型与联调方式。
- **新增文档**：`docs/前后端接口对齐文档.md（前后端接口对齐文档）`
- **抽取依据**：以真实代码为准，主要来自 `backend/src/types/interview.ts`（面试流式类型定义）、`backend/src/types/report.ts`（报告类型定义）、`backend/src/types/practice-pool.ts`（题池类型定义）、各 `controllers`（控制器）实现，以及 `frontend/src/services/sse/interview-stream.ts`（前端流式消费实现）。
- **关键结论**：
  - 对外主链路仍以 `POST /api/interview/stream`（模拟面试流式接口）为核心，返回 `SSE`（服务端发送事件）流。
  - 推送事件名固定为 `chunk` / `done` / `error`，其中 `chunk` 事件 JSON（结构化数据）字段固定为 `content`。
  - 当前系统无鉴权、无应用层心跳、无自动重连；前端支持 `AbortController`（中止控制器）主动取消流。
  - 文档中已为所有核心字段补了示例值，便于直接发给 C++ 同学修改替换。

- **2026-06-13**：重写 README.md 首页展示区，改成更适合 GitHub 对外展示的封面式布局，并保留原有正文内容。
- **2026-06-13**：继续精简 README.md 后半部分，删除冗长的大模型说明，改为模拟面试流式接入示例、会话/报告接口示例和可选模型配置说明。
- **2026-06-13**：为 README 新增本地图片目录 `docs/readme-images/`（README 图片目录），并把首页封面改为相对路径，便于后续替换为项目截图。
- **2026-06-13**：把 README.md 的接入说明改成面向使用者的启动流程，强调先配 `frontend/.env`（前端环境变量文件） 再启动后端和前端即可进入模拟面试对话。
- **2026-06-13**：收敛 README 对外入口，仅保留宇宙模拟面试页路径，移除其他页面路径，避免用户访问未完成页面。
- **2026-06-13**：补充 README（项目说明文档）写法约束，统一要求正文优先中文主句，英文术语第一次出现必须带中文括号注释，路径和命令优先放代码块，减少 GitHub（代码托管平台）展示时的跑位和断句问题。
- **2026-06-20**：修订 `docs/AI模拟面试模块-可靠性升级方案.md`（可靠性升级方案文档），将原先偏 `EventSource`（浏览器原生 SSE 对象）的设计改为匹配当前 `POST + fetch + ReadableStream`（提交请求加可读流）链路的渐进增强方案；明确保留 `interview-stream.ts`（面试流服务门面）、复用 `request.ts`（请求工具），并把 `idempotentKey`（幂等键）与 `checkpoint`（断点快照接口）列为需要三方对齐的后端协作能力。
- **2026-06-20**：开始落地可靠性升级 Phase 1（第一阶段）：新增 `frontend/src/services/storage/interview-message-store.ts`（面试消息存储服务），在 `useInterviewStream.ts`（面试流组合函数）内统一持久化 `InterviewMessage`（面试消息）；宇宙页通过 `setActiveSessionId`（设置当前会话标识函数）绑定会话，刷新后可从 `localStorage`（浏览器本地存储）恢复消息，历史 `streaming`（流式生成中）消息恢复为 `aborted`（已中止）以避免误显示仍在生成。
- **2026-06-20**：落地可靠性升级 Phase 2（第二阶段）前端可实施部分：新增 `frontend/src/services/sse/fetch-stream-transport.ts`（fetch 流传输适配），保留 `interview-stream.ts`（面试流服务门面）并把远程流请求统一交给传输适配处理；错误分类覆盖 `HTTP`（超文本传输协议）错误、网络错误、空响应体和主动取消，并通过前端内部 `activity`（连接活动事件）记录最后收流时间。`useInterviewStream.ts`（面试流组合函数）新增运行时 `streamConnectionHint`（流连接提示），远程流超过 15 秒无活动时只提示“可能无响应”，不自动重连、不修改 `POST /api/interview/stream`（面试流式接口）契约、不新增后端字段。
- **2026-06-20**：Phase 2（第二阶段）验证：当前环境缺少全局 `pnpm`（包管理器）和 `corepack`（包管理器代理），改用本地 `..\node_modules\.bin\vite.cmd build`（执行前端生产构建）在 `frontend/`（前端目录）下验证通过；构建仅输出既有 `chunk`（代码分包）体积提示。对本次改动文件执行乱码片段扫描，未发现 `鍘`、`褰`、`绛`、`鎸`、`寰`、`宸`、`鏂`、`棰`、`瀵`、`鐘` 等片段。
- **2026-06-20**：确认 `docs/AI模拟面试模块-可靠性升级方案.md`（可靠性升级方案文档）中 Phase 2（第二阶段）目标已由现有代码覆盖后，继续落地 Phase 3（第三阶段）前端范围：扩展 `frontend/src/utils/request.ts`（请求工具）支持按请求配置 `retry`（重试配置）、统一 `errorType`（错误类型）分类与网络/超时/取消场景归一；不新增第二套 `axios`（HTTP 请求库）实例。
- **2026-06-20**：将 `frontend/src/services/interview/interview-session-api.ts`（面试会话接口）、`frontend/src/services/interview/interview-report-api.ts`（面试报告接口）、`frontend/src/services/practice/practice-pool-api.ts`（专项题池接口）从直接 `fetch`（网络请求接口）切换为统一复用 `request.ts`（请求工具），并为非流式 `REST API`（接口）补齐请求级重试与统一错误处理入口。
- **2026-06-20**：Phase 3（第三阶段）验证：使用 `..\node_modules\.bin\vite.cmd build`（执行前端生产构建）在 `frontend/`（前端目录）下验证通过；本次无新增构建错误，仅保留既有 `chunk`（代码分包）体积提示。
- **2026-06-21**：新增 `docs/33.后台管理模块-会话报告与数据看板方案.md`（后台管理模块方案文档），明确第一阶段以会话管理、报告管理、轻量数据看板、登录态与 `user/admin`（普通用户/管理员）角色区分为主；方案约束不改现有 `SSE`（服务端发送事件）流式契约，优先复用现有会话与报告接口，后续再升级真实鉴权和后台聚合接口。
- **2026-06-21**：落地后台管理模块 Phase 1（第一阶段）前端原型：新增 `/admin/login`（后台登录页）、`/admin/dashboard`（后台数据看板页）、`/admin/sessions`（后台会话管理页）、`/admin/reports`（后台报告管理页）；扩展本地登录态支持 `user/admin`（普通用户/管理员）角色区分，路由守卫按 `requiredRole`（所需角色）拦截后台路由，并在普通登录页增加后台入口。
- **2026-06-21**：继续推进后台管理模块 Phase 1（第一阶段）缺口：为 `frontend/src/views/admin/sessions.vue`（后台会话页）补充“查看详情”面板，复用 `getRemoteInterviewSessionDetail`（获取远端会话详情接口）展示消息链路；为 `frontend/src/views/admin/reports.vue`（后台报告页）补充“查看详情”面板，复用 `getRemoteInterviewReportBySessionId`（按会话获取远端报告接口）展示摘要、弱项标签和建议聚焦，不新增接口、不改现有前后端契约。
- **2026-06-21**：修正后台管理页交互：`查看详情` 现已支持再次点击或面板按钮执行 `收回详情`，`重新加载` 会在刷新列表后同步刷新当前展开详情；同时补充后台筛选栏。注意：当前后端真实契约未提供 `status`（状态字段）、`score`（评分字段）、`generateStatus`（生成状态字段），因此 `frontend/src/views/admin/sessions.vue`（后台会话页）的状态筛选与 `frontend/src/views/admin/reports.vue`（后台报告页）的分数区间 / 生成状态筛选均为前端基于现有列表字段的派生映射，不代表后端已提供同名字段。
- **2026-06-21**：开始落地后台管理模块 Phase 2（第二阶段）真实后端能力：在现有 `backend/`（Node.js 后端目录）内增量新增 `auth`（鉴权）与 `admin`（后台聚合）路由，补充 `POST /api/auth/login`（登录接口）、`GET /api/auth/me`（当前用户接口）、`GET /api/admin/dashboard`（后台看板聚合接口）、`GET /api/admin/sessions`（后台会话聚合列表接口）、`GET /api/admin/reports`（后台报告聚合列表接口）；实现后端 `Bearer Token`（请求头令牌）校验与 `admin`（管理员）角色守卫，并将前端 `useAuth.ts`（登录组合函数）、`frontend/src/views/admin/dashboard.vue`（后台看板页）、`frontend/src/views/admin/sessions.vue`（后台会话页）、`frontend/src/views/admin/reports.vue`（后台报告页）切换到真实后端接口。当前仍未实现“普通用户仅查看自己的数据”的真实隔离，因为现有会话 / 报告存储结构尚无 `owner`（归属用户）字段，需后续单独补齐存储契约。
- **2026-06-21**：修复 `request.ts`（统一请求工具）基础地址策略，避免新接口默认打到 `frontend/src/utils/location.ts`（地址配置工具）里硬编码的 `baseApi`（基础接口地址）。现在改为：优先读取 `VITE_API_BASE_URL`（显式环境变量），开发环境默认走 `window.location.origin + /api`（当前域名下的本地代理），只有非开发环境且未配置环境变量时才回退到历史兜底地址。这样后续新建服务文件只要复用 `request.ts`，默认就会命中本地 `Vite`（前端开发服务器）代理，不会再出现后台登录这类请求绕过本地 `Node.js`（后端运行时）的超时问题。
- **2026-06-21**：修复后台登录前认证探测崩溃：`frontend/src/services/auth/auth-api.ts`（认证接口文件）中的 `fetchCurrentAuthSession`（获取当前登录会话函数）在 `/api/auth/me`（当前用户接口）返回 `401`（未授权）时，原先会对 `null` 响应直接读取 `user`（用户字段）并触发前端异常。现改为将该分支统一视为“未登录”，并在 `frontend/src/router/permission.ts`（路由前置守卫）里补充 `try/catch`（异常捕获）降级，避免旧本地登录态或过期 `token`（令牌）导致后台登录页白屏/无法继续登录。
- **2026-06-21**：继续修复后台真实登录链路：排查发现 `frontend/src/utils/request.ts`（统一请求工具）请求拦截器此前把 `Authorization`（鉴权请求头）直接写成裸 `token`（令牌），而 `backend/src/middlewares/auth-middleware.ts`（鉴权中间件）只接受 `Bearer token`（带 Bearer 前缀的令牌），导致 `/api/auth/login`（登录接口）成功后，`/api/auth/me`（当前用户接口）和后台聚合接口仍持续返回 `401`（未授权）。现已统一改为发送 `Authorization: Bearer <token>`（标准 Bearer 鉴权头），并保留登录接口不注入旧令牌，避免新旧登录态串扰。
- **2026-06-21**：重做后台管理视觉主题，统一切换为贴近宇宙页的深空舱室风格：更新 `frontend/src/views/admin/layout.vue`（后台布局页）、`frontend/src/views/admin/dashboard.vue`（数据看板页）、`frontend/src/views/admin/shared-admin-table.scss`（后台表格共享样式），将浅色后台改为深色星际背景、冷青蓝高光、玻璃拟态卡片与更明显的激活态；同时将导航、页头、表头、表格正文、筛选器、详情卡片、按钮与标签字号整体抬高，解决原后台“字体太小看不清”的可读性问题。
- **2026-06-21**：继续微调后台宇宙主题的可用性：应用户反馈，将后台字体整体从上一版回调一档，避免标题与表格信息过大；同时调整 `frontend/src/views/admin/layout.vue`（后台布局页）为固定高度双栏结构，新增 `admin-content-scroll`（右侧内容滚动容器），使桌面端仅右侧内容区滚动、左侧菜单栏保持固定不随内容滚动；并将滚动条改为深色弱化样式，避免原生白色滚动条在深色背景下过于突兀。
- **2026-06-21**：继续收口后台侧栏底部控制区样式：移除顶部身份横栏后，按用户反馈把左下控制区从“外层整块面板 + 上下堆叠”改成“左右并排”结构，去掉多余的“当前身份 / 控制席身份”文案与重复身份显示；现在左侧仅保留一个气泡身份徽标（管理员），右侧为独立退出按钮，且不再额外包裹外层大框。
- **2026-06-21**：继续推进后台管理模块 Phase 2（第二阶段）真实数据隔离：为 `backend/src/storage/interview-session-store.ts`（面试会话存储）与 `backend/src/types/report.ts`（报告类型定义）补充可选 `owner`（归属用户）字段，新增 `attachOptionalAuth`（可选鉴权中间件）挂到 `GET/POST /api/interview/*`（面试接口）链路，使已登录普通用户发起面试流时可自动写入 `owner`，匿名历史数据继续兼容。
- **2026-06-21**：同步收口普通用户数据范围：`backend/src/controllers/interview-controller.ts`（面试控制器）与 `backend/src/controllers/report-controller.ts`（报告控制器）现在会按 `authUser.role`（登录用户角色）决定返回范围，`user`（普通用户）只可查看自己名下会话 / 报告详情与列表，`admin`（管理员）仍保留全量视角；不匹配归属的详情请求统一返回 `404`（未找到），避免暴露资源存在性。
- **2026-06-21**：补齐匿名访问边界：未登录请求现在只能读取历史无 `owner`（归属用户）的旧会话 / 旧报告；一旦数据已绑定 `owner`，未登录用户与其他普通用户都无法查看，也不能基于 чужести `sessionId`（会话标识）触发报告生成。这样既兼容旧本地数据，又让第二阶段真实隔离对新数据生效。
- **2026-06-22**：新增 `docs/35.SSE流式对话三层解耦架构改造方案.md`（SSE 流式三层解耦方案文档），明确当前流式对话仍是“两层半”结构；方案建议在不修改 `POST /api/interview/stream`（面试流式接口）与 `chunk/done/error`（分片/完成/错误）契约的前提下，拆出 `StreamController`（流控制器）、`ConnectionManager`（连接管理器）、`stream-event-router`（流事件路由）与 `stream-heartbeat`（流心跳工具），并把 `prompt`（提示词）组装和 `mock`（本地模拟）生成从 `interview-stream.ts`（面试流门面文件）外移。
- **2026-06-22**：根据 `docs/35.SSE流式对话三层解耦架构改造方案.md`（SSE 流式三层解耦方案文档）开始落地前端改造：新增 `frontend/src/services/sse/interview-stream-controller.ts`（面试流控制器文件）、`stream-connection-manager.ts`（流连接管理器文件）、`stream-event-router.ts`（流事件路由文件）、`stream-heartbeat.ts`（流心跳工具文件）、`interview-stream-payload.ts`（面试流请求组装文件）与 `interview-stream-mock.ts`（面试流本地模拟文件）；将 `interview-stream.ts`（面试流门面文件）收敛为兼容导出层，`useInterviewStream.ts`（面试流组合函数）不再直接持有底层 `AbortController`（中止控制器）与健康检查计时器，改为消费 `InterviewStreamTask`（面试流任务对象）快照与被动 `heartbeat`（心跳检测）结果。当前仍保持不改 `POST /api/interview/stream`（面试流式接口）和 `chunk/done/error`（分片/完成/错误）契约、不自动重连。
- **2026-06-22**：继续推进 35 号方案 Phase 3（第三阶段）前端范围：为 `stream-heartbeat.ts`（流心跳工具文件）补充标准化 `StreamHeartbeatState`（流心跳状态对象）输出，让不活跃提示统一由流控制层状态语义驱动；新增 `frontend/src/services/sse/interview-stream-error.ts`（流错误文案文件）集中处理 `remote/mock`（远程/本地模拟）错误提示文案，新增 `frontend/src/services/message/interview-stream-message-finalizer.ts`（流消息收尾处理器文件）统一处理 `done/error/aborted`（完成/错误/中止）消息收尾，进一步收缩 `useInterviewStream.ts`（面试流组合函数）的分支职责。
- **2026-06-22**：进入 35 号方案 Phase 4（第四阶段）前端预埋层。因当前仓库仍未提供后端 `heartbeat`（心跳事件）、`idempotentKey`（幂等键）与 `checkpoint`（断点快照）能力，暂不实现自动重连或自动恢复，也不改 `POST /api/interview/stream`（面试流式接口）契约。前端先新增 `frontend/src/services/sse/interview-stream-recovery.ts`（流恢复预埋文件），统一声明当前恢复能力开关、记录 `sessionStorage`（会话级存储）中的待恢复流上下文，并在 `useInterviewStream.ts`（面试流组合函数）内补齐“检测到上一轮本地中断但尚不支持自动恢复”提示与 `interrupted`（中断）型手动恢复入口，为后续三方对齐后接入真正的断点恢复留出稳定挂点。
- **2026-06-22**：按“先 Node、后 C++”策略推进 Phase 4（第四阶段）真实能力第一版：在 `backend/src/types/interview.ts`（面试流类型文件）内扩展可选 `idempotentKey`（幂等键）、`heartbeat`（心跳事件）与 `InterviewStreamCheckpointSnapshot`（断点快照类型）；新增 `backend/src/storage/interview-stream-checkpoint-store.ts`（流断点快照存储文件），将流式内容按 `sessionId + threadId + idempotentKey`（会话 + 线程 + 幂等键）持久化到 `backend/data/interview-stream-checkpoints.json`（流断点数据文件）；为 `streamInterviewController`（面试流控制器）补充周期性 `heartbeat`（心跳事件）和 `checkpoint`（断点快照事件）输出，并新增 `GET /api/interview/sessions/:sessionId/:threadId/checkpoint`（流断点快照查询接口）。前端同步扩展 `frontend/src/services/sse/sse-types.ts`（前端流类型文件）、`stream-event-router.ts`（流事件路由文件）、`interview-stream-controller.ts`（面试流控制器文件）与 `useInterviewStream.ts`（面试流组合函数），开始消费 `heartbeat/checkpoint`（心跳/断点）事件并维护 `activeIdempotentKey`（当前幂等键）与 `checkpointSequence`（当前断点序号）。当前仍未启用自动恢复，待 C++（后端语言）实现同名能力后可平滑切换 `Provider`（提供器）实现。
- **2026-06-22**：继续收口 Phase 4（第四阶段）前端恢复入口：新增 `frontend/src/services/sse/interview-stream-checkpoint-recovery.ts`（断点恢复服务文件），将“查询 `checkpoint API`（断点快照接口）并提取历史内容”的逻辑从 `useInterviewStream.ts`（面试流组合函数）中拆出；为 `frontend/src/services/message/interview-stream-message-finalizer.ts`（流消息收尾处理器文件）补充 `finalizeRestore`（恢复收尾函数），统一处理“补渲染旧内容后标记为 `aborted`（已中止）”的消息收尾。现在 `retryStream`（重试流函数）在 `interrupted`（中断）分支会优先调用 `checkpoint API`（断点快照接口），命中后只补渲染上次已生成内容，不自动续流；如需继续生成，用户需重新提交本轮回答。`error/aborted`（错误/主动中止）分支仍保持原有重新发流策略。
- **2026-06-22**：修复刷新后“恢复本轮反馈”入口丢失：根因是 `useInterviewStream.ts`（面试流组合函数）在刷新重新绑定会话时，`setActiveSessionId`（设置当前会话标识函数）会无条件清空 `retryableState`（可重试流状态），同时组件卸载时 `stopStream`（停止流函数）会清掉 `sessionStorage`（会话级存储）里的待恢复上下文。现在刷新卸载会保留 `pendingRecovery`（待恢复流上下文），重新绑定同一 `sessionId`（会话标识）时恢复 `interrupted`（中断）重试状态，并在 `SpaceMockScene.vue`（模拟面试场景组件）底部操作区补充同一个 `retry`（重试事件）入口；提示和按钮都按当前会话过滤，避免串到其他会话。验证：`vite build`（生产构建）通过，定向 `eslint`（代码规范检查）无硬错误，仅保留该场景组件既有模板缩进警告；乱码扫描未命中。
- **2026-06-22**：新增 `docs/36.HTTP请求层高可用增强方案.md`（HTTP 请求层高可用增强方案文档），收敛 `request.ts`（统一请求工具）后续增强方向；文档明确三项核心能力为 `TokenBucket`（令牌桶）并发控制、全局默认 `retry`（重试）策略与 `circuit breaker`（熔断器）重试熔断，并约束本次方案仅覆盖非流式 `REST API`（接口）链路，不纳入 `SSE`（服务端发送事件）流式通道。
- **2026-06-22**：开始按 `docs/36.HTTP请求层高可用增强方案.md`（HTTP 请求层高可用增强方案文档）落地 `frontend/src/utils/request.ts`（统一请求工具）增强：在单一 `axios`（HTTP 请求库）实例内补齐全局默认 `retry`（重试）策略、`TokenBucket`（令牌桶）并发队列与按“方法 + 路径”维度隔离的 `circuit breaker`（熔断器）状态机；新增 `queue_timeout`（排队超时）与 `circuit_open`（熔断打开）错误分类，并通过统一收尾路径保证并发令牌在成功、失败、取消、熔断拦截场景下都能释放，避免请求队列假死。
- **2026-06-22**：同步收敛服务层的局部默认 `retry`（重试）配置：将 `admin-api.ts`（后台接口文件）、`interview-session-api.ts`（面试会话接口文件）、`interview-report-api.ts`（面试报告接口文件）里的只读 `GET`（读取请求）默认重试移回 `request.ts`（统一请求工具）托管；保留显式关闭或显式传入的写请求配置，避免 `POST`（提交请求）/ 清空 / 生成类接口被隐式重复提交。
- **2026-06-22**：本轮验证：使用 `..\node_modules\.bin\vite.cmd build`（执行前端生产构建）在 `frontend/`（前端目录）下构建通过；未新增构建错误，仅保留既有大体积 `chunk`（代码分包）提示。当前这轮改动尚未单独提交，工作区保留 `request.ts`（统一请求工具）增强与文档更新待后续暂存。
- **2026-06-22**：新增 `docs/前端实习面试问答-五大核心实现-场景题速答版.md`（五大核心实现面试速答文档），基于真实代码实现收敛五个核心点：`SSE`（服务端发送事件）三层解耦、`rAF`（按帧调度）消息渲染队列、`Markdown`（标记语言）安全渲染、消息持久化、`HTTP`（超文本传输协议）请求层高可用；同时补充 30 秒回答、每个核心点 5 道场景题、已被追问的高频题整合答案，以及闭包（Closure，闭包）在并发控制和消息队列中的项目化说法。
- **2026-06-22**：更新 `docs/前端实习面试问答-五大核心实现-场景题速答版.md`（五大核心实现面试速答文档）：在五个“实现定位”下补充核心实现代码片段和中文注释，便于对照 `stream-event-router.ts`（流事件路由文件）、`interview-message-queue.ts`（面试消息队列文件）、`MarkdownMessage.vue`（Markdown 消息组件）、`interview-message-store.ts`（面试消息存储文件）与 `request.ts`（统一请求工具文件）讲实现；重写第六部分为当前项目真实高频追问，覆盖后台管理、`user/admin`（普通用户/管理员）权限、`owner`（归属用户）数据隔离、断点恢复、请求高可用和 C++（后端语言）协作边界；为第七部分闭包（Closure，闭包）题补充项目代码例子。
- **2026-06-22**：继续调整 `docs/前端实习面试问答-五大核心实现-场景题速答版.md`（五大核心实现面试速答文档）第一部分，使其更贴合简历表述“`SSE`（服务端发送事件）流式对话解耦”：补充与简历写法的对应关系、三层架构图、传输层 / 控制层 / 处理层职责表，并重写第一组核心代码注释，突出 `interview-stream-controller.ts`（面试流控制器文件）、`stream-event-router.ts`（流事件路由文件）与 `useInterviewStream.ts`（面试流组合函数）之间的职责边界。
- **2026-06-25**：修复普通登录进入宇宙页首屏的两处观感问题：`frontend/src/views/login.vue`（登录页）在登录成功后立即进入本地跃迁遮罩态，避免首登时目标页懒加载期间仍停留在登录表单；`frontend/src/views/showcase/mock-interview-space.vue`（宇宙模拟面试页主文件）把 `welcome=1`（欢迎入场标记）提前转成初始化态，使 `isCosmosBigBangPending`（大爆炸等待态）、`isCosmosContentRevealed`（宇宙内容展示态）和 `isScrollCapsuleRevealSettled`（滚动胶囊入场完成态）首帧即一致，并在大爆炸正式播放前隐藏 `SpaceVisualStage`（星球舞台组件），避免进入大爆炸动画前闪出一帧完整星球总览。验证：执行 `..\node_modules\.bin\vite.cmd build`（前端生产构建）通过；本机未识别全局 `pnpm`（包管理器命令），因此未直接执行 `pnpm build`。
